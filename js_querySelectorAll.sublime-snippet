<snippet>
  <content><![CDATA[
var ${1:elements} = document.querySelectorAll('${2:selector}');
Array.prototype.forEach.call(${1:elements}, function(el, i){
  ${3://code}
});
]]></content>
  <!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
  <tabTrigger>_querySelectorAll</tabTrigger>
  <!-- Optional: Set a scope to limit where the snippet will trigger 
  <scope>source.html</scope>
    -->
</snippet>